{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vella://contracts/v1/icd.json.schema.json",
  "title": "VELLA SDK Interface Control Document v1",
  "description": "Describes the embedded-SDK interface as specified in spec/icd.md \u00a74.1 and \u00a74.2. The schema describes the canonical Node/JSON form (camelCase keys). The Python SDK accepts and returns equivalent snake_case keys: intent, evidence_mask, authority_scope, policy_version, proof_signing_key for the request; decision, reason_code, latency_us, proof_bundle, proof_error for the response. Both key forms describe the same interface.",
  "type": "object",
  "definitions": {
    "IntentParams": {
      "type": "object",
      "description": "Normalized numeric form of the parameters supplied to govern(...). Resolve SDK symbolic/decimal convenience forms against the active policy before applying this schema. See spec/icd.md \u00a74.1 and spec/input-validation.md.",
      "required": [
        "intent",
        "evidenceMask"
      ],
      "properties": {
        "intent": {
          "type": "string",
          "description": "Action intent identifier (e.g., EXECUTE_CHANGE, ESCALATE_PRIVILEGE, DATA_EXPORT)."
        },
        "evidenceMask": {
          "type": "integer",
          "minimum": 0,
          "maximum": 4294967295,
          "description": "Normalized unsigned 32-bit evidence mask. SDK decimal and symbolic convenience forms must be resolved against the active policy before validating this representation. See spec/input-validation.md."
        },
        "authorityScope": {
          "type": "string",
          "description": "Named scope in the compiled policy; defaults to the policy's declared defaultScope."
        },
        "policyVersion": {
          "type": "string",
          "description": "Must match the compiled policy's version string; if omitted, the compiled version is used."
        },
        "proof": {
          "type": "object",
          "description": "Optional proof-signing configuration.",
          "properties": {
            "signingKey": {
              "type": "string",
              "description": "PEM-encoded private key. If provided, a signed proof bundle is included in the response."
            }
          }
        },
        "action": {
          "type": [
            "object",
            "null"
          ],
          "description": "Application-supplied effective action context; default null."
        },
        "evidence": {
          "type": [
            "object",
            "null"
          ],
          "description": "Application-supplied evidence context; default null."
        },
        "requestId": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1024
        },
        "boundary": {
          "enum": [
            "evaluation",
            "client-dispatch",
            "server-handler"
          ]
        },
        "buildHash": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "DecisionResponse": {
      "type": "object",
      "description": "The decision result returned synchronously by the SDK. See spec/icd.md \u00a74.2.",
      "required": [
        "decision",
        "reasonCode",
        "latencyUs"
      ],
      "properties": {
        "decision": {
          "type": "string",
          "enum": [
            "ALLOWED",
            "DENIED"
          ],
          "description": "The enforcement outcome."
        },
        "reasonCode": {
          "type": "string",
          "description": "Machine-readable reason for the decision (see spec/icd.md \u00a75.2)."
        },
        "latencyUs": {
          "type": "number",
          "description": "Microsecond elapsed time through evaluation, before optional signing."
        },
        "proofBundle": {
          "anyOf": [
            {
              "$ref": "proof.json"
            },
            {
              "$ref": "proof-v2.json"
            },
            {
              "type": "null"
            }
          ],
          "description": "Signed proof bundle when signing succeeds; null when requested signing fails."
        },
        "proofError": {
          "type": "string",
          "description": "Diagnostic string; present when signing was requested but failed."
        }
      }
    }
  },
  "properties": {
    "version": {
      "type": "string",
      "const": "v1"
    },
    "request": {
      "$ref": "#/definitions/IntentParams"
    },
    "response": {
      "$ref": "#/definitions/DecisionResponse"
    }
  }
}
