# Test Vectors

This directory contains canonical valid and tampered proof bundles used for verifier testing and CI.

- `valid/` bundles must verify successfully
- `tampered/` bundles must fail verification
